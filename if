==================================================================================
#1번 문제
==================================================================================

#include <stdio.h>

int main ()
{
    int a,b;
    scanf("%d %d",&a,&b);
    if (a>b)
    {   
        printf(">");
    }
    if (a==b)
    {   
        printf("==");
    }    
    if (a<b)
    {   
        printf("<");
    }
}
==================================================================================
#2번 문제
==================================================================================
#include <stdio.h>

int main()
{
    int a;
    scanf("%d",&a);
    if (90<=a&&a<=100)
    {
        printf("A");
    }
    if (80<=a&&a<90)
    {
        printf("B");
    }
    if (70<=a&&a<80)
    {
        printf("C");
    }
    if (60<=a&&a<70)
    {
        printf("D");
    }
    if (a<60)
    {
        printf("F");
    }
    return 0;
}

==================================================================================
#5번 문제
==================================================================================
#include <stdio.h>

int main ()
{
    int hour,min,total;
    scanf("%d %d",&hour,&min);
    total=60*hour+min;
    if (total<45)
    {
        total+=1440;
    }
    total-=45;
    printf("%d %d",total/60,total%60);
    return 0;
}
